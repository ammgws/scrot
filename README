20170327
Edited scrot v18 from Debian repo to fix the rectangle glitch issue when using scrot to select an area (see issue #1).


Compile:

$ ./configure
$ make
$ su -c "make install"

(Red hat users, use $ ./configure --prefix=/usr)
